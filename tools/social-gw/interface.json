{
  "name": "social-gw",
  "description": "Unified social posting and comment retrieval wrapper built on top of the gw tool.",
  "capability_summary": "Routes cross-platform social actions (YouTube, TikTok, X) through gw with deterministic JSON results for channel operations.",
  "parameters": [
    {
      "name": "command",
      "type": "string",
      "description": "Command name. Supported: post, fetch-comments."
    },
    {
      "name": "provider",
      "type": "string",
      "description": "Target platform provider (youtube, tiktok, x)."
    },
    {
      "name": "target_id",
      "type": "string",
      "description": "Platform-specific target identifier (video/thread/post/comment id)."
    },
    {
      "name": "text",
      "type": "string",
      "description": "Text content for posting or replying actions."
    }
  ]
}
