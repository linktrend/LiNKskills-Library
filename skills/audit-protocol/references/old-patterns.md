# Old Patterns

- Executing write actions without prior intent log removes audit traceability.
- Logging intent after write action is non-compliant.
- Missing rollback cues in intent logs weakens incident response.
