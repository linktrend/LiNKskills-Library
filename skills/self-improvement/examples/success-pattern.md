# Success Pattern

Scenario: rising validation failures in two skills over last 30 runs.

1. Agent parses ledger and identifies repeated schema mismatch class.
2. Agent links mismatch to loose input contracts in both skills.
3. Agent proposes contract tightening plus one migration note per skill.
4. Agent outputs prioritized plan with rollback path and expected impact.
