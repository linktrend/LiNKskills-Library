# Success Pattern

Scenario: "Design a 3-week launch program spanning product, marketing, and analytics."

1. Agent splits objective into three subdomains.
2. Agent decomposes each subdomain into atomic tasks with explicit inputs/outputs.
3. Agent marks cross-domain dependencies and parallel paths.
4. Agent delivers an ordered plan with verification criteria for each atomic step.
