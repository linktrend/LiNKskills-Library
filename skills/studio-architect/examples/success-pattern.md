# Example Trace: Success Pattern

## Scenario
Operator requests a B2B SaaS portal with role-based access and billing.

## Trace
- Skill reads `factory.json` and classifies run as `Specialist`.
- Baseline evaluation picks SaaS Starter Kit.
- Output includes startup sequence, core modules, and custom delta list.
- Contract passes and run is logged.
