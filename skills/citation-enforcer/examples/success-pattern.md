# Success Pattern

Scenario: user asks for a performance summary with factual metrics.

1. Agent extracts 8 claims from draft.
2. Agent maps each claim to Memory, Search, or File source.
3. Agent adds citation table with source pointers and confidence labels.
4. Agent blocks one unsupported claim and removes it before final output.
